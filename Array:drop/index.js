let commonIndex = require(__dirname + '/../common');

module.exports = {
    problem: commonIndex.problem('Array:drop'),
    solution: commonIndex.solution('Array:drop'),
    verify: commonIndex.verify
}
