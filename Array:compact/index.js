let commonIndex = require(__dirname + '/../common');

module.exports = {
    problem: commonIndex.problem('Array:compact'),
    solution: commonIndex.solution('Array:compact'),
    verify: commonIndex.verify
}
