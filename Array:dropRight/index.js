let commonIndex = require(__dirname + '/../common');

module.exports = {
    problem: commonIndex.problem('Array:dropRight'),
    solution: commonIndex.solution('Array:dropRight'),
    verify: commonIndex.verify
}
